---
title: Code PathFinder
description: Open-source AI-powered security analysis combining LLM-assisted reviews with structural queries.
template: splash
hero:
  tagline: "AI-powered code security for modern teams."
  actions:
    - text: Install Now
      link: /quickstart
      icon: right-arrow
      variant: primary
    - text: SecureFlow AI
      link: /secureflow-ai
      icon: rocket
    - text: Browse Rules
      link: /atlas
      icon: open-book
    - text: Tech Blog
      link: /blog
      icon: pen
---

import { Card, CardGrid, Icon } from '@astrojs/starlight/components';
import PostHogLayout from '../../layouts/PostHogLayout.astro';
import { getCollection } from 'astro:content';
import '../../styles/motion-gradient.css';
import '../../styles/blog-cards.css';
import '../../styles/feature-cards.css';

export const allPosts = await getCollection('docs', ({ id }) => {
  if (id.startsWith('blog/index.mdx')) return false;
  return id.startsWith('blog/');
});

export const latestPosts = [...allPosts]
  .sort((a, b) => new Date(b.data.pubDate || '1970-01-01') - new Date(a.data.pubDate || '1970-01-01'))
  .slice(0, 6);

<PostHogLayout>
</PostHogLayout>

<div class="motion-gradient"></div>

<div style="display: flex; justify-content: center;">
```bash
Code-PathFinder Console:
> FROM method_declaration AS md
  WHERE md.getVisibility() == "public" && md.getName() == "main"
  SELECT md, "Listing all main methods"

public static void main(String[] args) {
    System.out.println("Set thy heart upon thy work,");
    System.out.println("but never on its reward. - Bhagavad Gita, Chapter 2, Verse 47");
}
```

</div>

<br />
<div style="text-align: center;">
## Supported Platforms
</div>

<div class="platform-icons" style="display: flex; justify-content: space-around; align-items: center; margin: 40px 0;">
  <Icon name="seti:docker" color="currentColor" size="4rem" />
  <Icon name="seti:github" color="currentColor" size="3rem" />
  <Icon name="bitbucket" color="currentColor" size="3rem" />
  <Icon name="gitlab" color="currentColor" size="3rem" />
  <svg width="4rem" height="4rem" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
    <path fill="currentColor" d="M17,4v9.74l-4,3.28-6.2-2.26V17L3.29,12.41l10.23.8V4.44Zm-3.41.49L7.85,1V3.29L2.58,4.84,1,6.87v4.61l2.26,1V6.57Z"/>
  </svg>
  <Icon name="seti:npm" color="currentColor" size="4rem" />
</div>

<br />

<br />
<div style="text-align: center;">
## Why Code Pathfinder
</div>

<div class="feat-grid">
  <div class="feat-card">
    <div class="feat-head">
      <Icon class="feat-icon" name="rocket" size="1.15rem" color="currentColor" />
      <h4>LLM Security Reviews</h4>
    </div>
    <p>Use leading models (Claude, GPT, Gemini, Grok) to catch risky patterns with context—beyond regex and grep.</p>
  </div>
  <div class="feat-card">
    <div class="feat-head">
      <Icon class="feat-icon" name="github" size="1.15rem" color="currentColor" />
      <h4>Editor, PRs and Pipelines</h4>
    </div>
    <p>See findings in your editor, pull requests and CI pipelines with a single configuration and consistent results.</p>
  </div>
  <div class="feat-card">
    <div class="feat-head">
      <Icon class="feat-icon" name="seti:pipeline" size="1.15rem" color="currentColor" />
      <h4>Code Graph Analysis</h4>
    </div>
    <p>Structural search, call graphs and source‑to‑sink tracing for precise, explainable detections.</p>
  </div>
  <div class="feat-card">
    <div class="feat-head">
      <Icon class="feat-icon" name="seti:plan" size="1.15rem" color="currentColor" />
      <h4>Reports and Integrations</h4>
    </div>
    <p>Export SARIF and DefectDojo with severity mapping and metadata for smooth triage and tracking.</p>
  </div>
</div>

<br/>
<br/>
<br/>
<div style="text-align: center;">
## Latest Blog Posts

</div>

<div class="blog-grid">
  {latestPosts.map((post) => (
    <div class="blog-card">
      <a class="blog-card-link" href={`/${post.slug}`}>
        <h4>{post.data.title}</h4>
        {post.data.description && (<p>{post.data.description}</p>)}
        <div class="blog-meta">
          {post.data.pubDate && (
            <span class="date">{new Date(post.data.pubDate).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}</span>
          )}
          <span>•</span>
          <span class="author">{post.data.author || 'Code Pathfinder Team'}</span>
          <span style="margin-left:auto"></span>
          <span>Read →</span>
        </div>
      </a>
    </div>
  ))}
</div>

<br />

<section
  style="
    position: relative;
    margin: 64px 0 24px 0;
    padding: 64px 16px;
    border-radius: 16px;
    overflow: hidden;
    background: radial-gradient(1200px 400px at 50% -10%, rgba(32,178,170,0.20), rgba(0,0,0,0));
    text-align: center;
  "
>
  <h2 style="margin: 0 0 12px 0; font-size: 1.9rem; font-weight: 700;">
    Try SecureFlow in your editor
  </h2>
  <p style="margin: 0 0 28px 0; opacity: 0.9;">
    Install the SecureFlow extension for VS Code or Open VSX and start analyzing your code in seconds.
  </p>
  <div style="display: inline-flex; gap: 12px; flex-wrap: wrap; justify-content: center;">
    <a
      href="https://marketplace.visualstudio.com/items?itemName=codepathfinder.secureflow"
      target="_blank"
      rel="noopener noreferrer"
      style="
        display:inline-flex; align-items:center; justify-content:center;
        padding: 10px 16px; border-radius: 10px; font-weight: 600;
        background: var(--sl-color-accent); color: var(--sl-color-bg);
        text-decoration:none; min-width: 220px;
      "
    >
      Visual Studio Code
    </a>
    <a
      href="https://open-vsx.org/extension/codepathfinder/secureflow"
      target="_blank"
      rel="noopener noreferrer"
      style="
        display:inline-flex; align-items:center; justify-content:center;
        padding: 10px 16px; border-radius: 10px; font-weight: 600;
        background: transparent; color: var(--sl-color-accent);
        border: 1px solid var(--sl-color-accent);
        text-decoration:none; min-width: 220px;
      "
    >
      Open VSX
    </a>
  </div>
</section>
