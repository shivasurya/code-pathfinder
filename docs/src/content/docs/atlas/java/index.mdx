---
title: Java - Code Pathfinder Atlas
template: splash
description: Language-specific rules for Java applications
---

import { Card, CardGrid, Icon } from '@astrojs/starlight/components';
import PostHogLayout from '../../../../layouts/PostHogLayout.astro';
import CollapsibleCode from '../../../../components/CollapsibleCode.astro';

export const sqlInjectionExample = `// ❌ Vulnerable: SQL Injection risk
String query = "SELECT * FROM users WHERE id = '" + userId + "'";

// ✅ Safe: Using PreparedStatement
String query = "SELECT * FROM users WHERE id = ?";
PreparedStatement stmt = conn.prepareStatement(query);
stmt.setString(1, userId);`;

<PostHogLayout/>

Browse our comprehensive collection of Java-specific rules designed to identify vulnerabilities and code quality issues in your Java applications.

## Featured Rules

<Card title="SQL Injection" icon="warning">
    **Severity: Critical**  
    Detects potential SQL injection vulnerabilities in Java applications where user input is directly concatenated into SQL queries.
    <CollapsibleCode 
        code={sqlInjectionExample}
        lang="java"
        title="SQL Injection Example"
        marks={['Vulnerable', 'Safe']}
    />
</Card>
 
  
  <Card title="Insecure Random" icon="warning">
    **Severity: High**  
    Identifies usage of java.util.Random for security-sensitive operations instead of SecureRandom.
    <CollapsibleCode 
      code={`// ❌ Vulnerable: Predictable random numbers
Random random = new Random();
byte[] token = new byte[32];
random.nextBytes(token);

// ✅ Safe: Cryptographically secure random
SecureRandom secureRandom = new SecureRandom();
byte[] secureToken = new byte[32];
secureRandom.nextBytes(secureToken);`}
      lang="java"
      title="Insecure Random Example"
      marks={['Vulnerable', 'Safe']}
    />
  </Card>
  
  <Card title="Hardcoded Credentials" icon="warning">
    **Severity: Critical**  
    Detects hardcoded credentials in Java source code that could be exposed in version control or decompiled code.
    <CollapsibleCode 
      code={`// ❌ Vulnerable: Hardcoded credentials
private static final String PASSWORD = "super_secret_123";

// ✅ Safe: Environment variables
String password = System.getenv("DB_PASSWORD");

// ✅ Safe: Configuration management
@Value("app.db.password")
private String configPassword;`}
      lang="java"
      title="Hardcoded Credentials Example"
      marks={['Vulnerable', 'Safe']}
    />
  </Card>
  
  <Card title="Unsafe Deserialization" icon="warning">
    **Severity: High**  
    Identifies unsafe deserialization of untrusted data that could lead to remote code execution.
    <CollapsibleCode 
      code={`// ❌ Vulnerable: Unsafe deserialization
ObjectInputStream ois = new ObjectInputStream(input);
Object data = ois.readObject(); // RCE risk

// ✅ Safe: Type-safe deserialization
ObjectMapper mapper = new ObjectMapper();
UserData data = mapper.readValue(json, UserData.class);`}
      lang="java"
      title="Unsafe Deserialization Example"
      marks={['Vulnerable', 'Safe']}
    />
  </Card>

## Rule Categories

- **Security Vulnerabilities**: Rules for detecting security issues like XSS, CSRF, and more
- **Performance Issues**: Rules for identifying performance bottlenecks
- **Code Quality**: Rules for ensuring code maintainability and readability
- **Best Practices**: Rules for enforcing Java best practices and conventions

## How to Use

To run these rules against your Java codebase:

```bash
code-pathfinder scan --lang java --rules java/security /path/to/your/project
```

For more information on using Code PathFinder with Java, see our [Java documentation](/docs/java).
